module.exports = {
  logo: "https://cn.vuejs.org/images/logo.png", // logo
  title: "数据中台", // title
  layout: "basic", // 页面布局，支持 sidemenu/topmenu/basic 三种布局，默认 basic
  navHeight: 64, // 导航高度，默认 64px
  navTheme: "dark", // 导航主题，支持 light/dark 两种主题，默认 dark
  sideWidth: 256, // 侧栏宽度，默认 256px
  sideTheme: "light", // 侧栏主题，支持 light/dark 两种主题，默认 light
  contentWidth: "fluid", // 内容区宽度，支持 fluid/fixed 两种方案，默认 fluid
  fixedHeader: true, // 固定导航栏，默认 true
  autoHideHeader: false, // 自动隐藏导航栏，默认 false
  fixSiderbar: false // 固定侧边栏，默认 true
};
